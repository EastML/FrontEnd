function repeatStringNumTimes (str, num) {
  var holder = [];
  
  for (var i = 0; i < num; i++) {
    holder.push([str]);
  }
  var output = holder.join("");
  return output;
}

repeatStringNumTimes ("abc", 3);
